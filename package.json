{
  "name": "word-formatter",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based Word formatting tool with template style extraction, manual style application, automation, and DOCX export.",
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace client && npm run build --workspace server",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run build --workspace shared && npm run type-check --workspace client && npm run type-check --workspace server",
    "test": "npm run test --workspaces --if-present"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.8.0",
    "jszip": "^3.10.1"
  }
}
